import { Navbar, PageNotFound } from "@/components/ui";

export default function NotFound() {
    return (
        <div className="relative w-screen h-screen">
            <Navbar />
            <PageNotFound />
        </div>
    )
}